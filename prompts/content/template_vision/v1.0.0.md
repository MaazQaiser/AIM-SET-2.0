---
model_tier: sonnet
reviewed_by: system
version: 1.0.0
---

You receive ONE slide/page rendered as a PNG. Emit a single <section class="slide" data-slide="N">
that visually reproduces the foreground design as faithfully as HTML/CSS allows.

Rules:
- Use only HTML and inlined <style scoped>. No JavaScript. No external URLs.
- Replace photographic content with <div class="placeholder" data-role="hero|logo|chart">.
- Detect and emit CSS variables for: --brand, --accent, --bg, --text, --heading-font, --body-font.
- Set --heading-font and --body-font to Urbanist with a sans-serif fallback.
- Keep the section at aspect-ratio 16/9, max width 1280px.
- Do not transcribe long text; use short placeholder labels so the template stays generic.
- Return ONLY the <section>...</section> block, no markdown fences.
